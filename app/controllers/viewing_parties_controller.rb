class ViewingPartiesController < ApplicationController 
  def new 
    # movie = MovieFacade.new 
    # @movie = movie.search_by_id(params[:movie_id])
    # # require 'pry'; binding.pry 
    # @user = User.all
    # viewing_party = viewing_party.create!
  end
end